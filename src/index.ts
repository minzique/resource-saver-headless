// Main exports for the resource-saver-headless library
export { BrowserHelper, ResourceContent, BrowserOptions, debug, enableDebug } from './browser';
export { resolveURLToPath, saveFile, URLPathInfo } from './utils';

// Default export for convenience
export { BrowserHelper as default } from './browser';
